node_modules
build
dist
.react-router
.env
.env.local
.env.dev
*.sqlite
*.sqlite-shm
*.sqlite-wal
# Embedded PGlite data directory (DURO_DB_PATH). The *.sqlite globs above only
# covered its old, misleading name.
pgdata/
*.log
.DS_Store
coverage/
.mcp.json
scripts/mcp/node_modules

# Duro session-hook cache (duro hook install)
.claude/.duro-session.cache*
