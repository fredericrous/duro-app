build/
.react-router/
node_modules/

# Chart YAML follows the charts repo yamllint conventions (two spaces before
# inline comments), which prettier rewrites to one — see the 1.52.0 publish
# failure. Helm templates are not valid standalone YAML for prettier anyway.
chart/

# Generated by `duro skill install`; byte-compared by --check, never reformatted
.claude/skills/duro-mockup/SKILL.md

# Mockup artboards: authored in the design-canvas shape and checked by `duro mockup check`
docs/mockups/
